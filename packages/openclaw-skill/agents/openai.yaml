interface:
  display_name: "SenseKit for Agents"
  short_description: "SenseKit hook setup, raw signal batches, and trusted agent actions"
  default_prompt: "Use $sensekit-openclaw to configure SenseKit webhook handling, explain incoming raw signal batches, and design a trusted local action flow for an AI agent."

policy:
  allow_implicit_invocation: true
