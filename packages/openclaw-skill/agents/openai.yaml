interface:
  display_name: "SenseKit for OpenClaw"
  short_description: "SenseKit hook setup and event handling"
  default_prompt: "Use $sensekit-openclaw to configure OpenClaw for SenseKit webhooks and interpret incoming SenseKit events safely."

policy:
  allow_implicit_invocation: true
